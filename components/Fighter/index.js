import { Flex } from "@chakra-ui/react";
import React from "react";

const Fighter = () => {
  return <Flex {...styles.wrapper}></Flex>;
};

export { Fighter };

// Styles

const styles = {
  wrapper: {},
};
