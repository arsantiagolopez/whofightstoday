import { Flex } from "@chakra-ui/react";
import React from "react";

const Upcoming = () => {
  return <Flex {...styles.wrapper}></Flex>;
};

export { Upcoming };

// Styles

const styles = {
  wrapper: {},
};
