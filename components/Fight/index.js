import { Flex } from "@chakra-ui/react";
import React from "react";

const Fight = ({ isHeadline }) => {
  return <Flex {...styles.wrapper}></Flex>;
};

export { Fight };

// Styles

const styles = {
  wrapper: {},
};
